fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
pydantic>=2.7,<3
python-multipart>=0.0.9,<1
# Real image/model inference:
pillow>=10,<13
onnxruntime>=1.18,<2
huggingface_hub>=0.24,<1
# Quantum + classical machine learning.
# With these installed the circuit runs through Qiskit Machine Learning's
# EstimatorQNN. Without them the app still runs and reports its backend as
# "Fallback quantum simulation - not Qiskit execution".
numpy>=1.26,<3
scikit-learn>=1.4,<2
qiskit>=2.1,<3
qiskit-machine-learning>=0.9,<1
# Tests (optional): pip install pytest httpx
# For a fully reproducible Qiskit environment, run `pip freeze > requirements.lock` after a successful install.
